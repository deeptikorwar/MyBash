echo "Let's try to create some useful scripts"
echo
echo "Type a folder path and then I will list its contents for you"
read dirpath
echo "The path you typed is" $dirpath
echo
echo "Note that you need to use eval command"
echo "so that the path is interpreted correctly"

eval ls $dirpath

echo
echo "See...scripts can be useful too"
